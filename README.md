# Library-System-E2E-Playwright
## วิธีรัน Automation Test (สำหรับเพื่อนในทีม)
1. เปิด Terminal ใน VS Code
2. พิมพ์ `npm install` (ทำครั้งแรกครั้งเดียว)
3. พิมพ์ `npx playwright test` เพื่อรันเทสทั้งหมด
4. พิมพ์ `npx playwright show-report` เพื่อดูรายงานผลการทดสอบ